<!-- j-redis-server logging. Override with -Dlogback.configurationFile=/path/to/logback.xml -->
<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{24} - %msg%n</pattern>
    </encoder>
  </appender>
  <!-- never block the command thread on logging: drop instead when the queue is full -->
  <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
    <queueSize>65536</queueSize>
    <discardingThreshold>0</discardingThreshold>
    <neverBlock>true</neverBlock>
    <appender-ref ref="CONSOLE"/>
  </appender>
  <logger name="io.netty" level="WARN"/>
  <root level="${JREDIS_LOG_LEVEL:-INFO}">
    <appender-ref ref="ASYNC"/>
  </root>
</configuration>
